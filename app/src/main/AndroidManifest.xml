<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="net.gongmingqm10.calendar">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme">
        <activity
            android:name=".activity.MainActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".activity.HorizontalCalendarActivity"
            android:label="@string/title_activity_horizontal_calendar"
            android:parentActivityName=".activity.MainActivity" />
        <activity
            android:name=".activity.VerticalCalendarActivity"
            android:label="@string/title_activity_vertical_calendar"
            android:parentActivityName=".activity.MainActivity" />
        <activity
            android:name=".activity.StartEndDateActivity"
            android:label="@string/title_activity_depart_return"
            android:parentActivityName=".activity.MainActivity" />
        <activity
            android:name=".activity.GoogleCalendarActivity"
            android:label="@string/title_activity_google_calendar"
            android:parentActivityName=".activity.MainActivity" />
    </application>

</manifest>
